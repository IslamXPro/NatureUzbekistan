<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item android:icon="@drawable/home_ic"
            android:title="Home"
            android:id="@+id/home_id"/>
        <item android:title="Search"
            android:id="@+id/search_id"
            android:icon="@drawable/search_ic_bl"/>
        <item android:title="Add"
            android:id="@+id/add_id"
            android:icon="@drawable/add_ic_bl"/>
        <item android:title="Favourite"
            android:id="@+id/favourite_id"
            android:icon="@drawable/favourite_ic_bl"/>
        <item android:title="@string/profile"
            android:id="@+id/profile_id"
            android:icon="@drawable/profile_ic"/>
    </group>

</menu>